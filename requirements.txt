# ===========================
# MagecoShipping Dependencies
# ===========================

# 🔍 Watchdog - monitoraggio cartelle in tempo reale
watchdog==4.0.0

# 🌐 Flask - interfaccia web locale per revisione/modifica ed export
Flask==3.0.3
Werkzeug==3.0.1

# 🖥️ Pystray - icona e menu nella System Tray di Windows
pystray==0.19.5

# 🖼️ Pillow - richiesto da Pystray per gestire le icone
Pillow==10.4.0

# ⚙️ Requests - per eventuali comunicazioni HTTP o API future
requests==2.32.3

# 📜 Colorama - per messaggi colorati nel terminale (debug/log)
colorama==0.4.6

# 📊 openpyxl - gestione file Excel (.xlsx)
openpyxl==3.1.2

# 🧮 pandas - manipolazione dati e creazione DataFrame per export Excel
pandas==2.2.2

# 📈 XlsxWriter - scrittura Excel con formattazioni avanzate
XlsxWriter==3.2.0

# 🧩 lxml - parsing FatturaPA XML
lxml==5.2.2

# 🗃️ sqlite-utils - gestione semplice di database SQLite
sqlite-utils==3.36.0

# 🔧 python-dateutil - gestione date e formattazioni
python-dateutil==2.9.0.post0

# 🪟 pywin32 - compatibilità con servizio Windows e API Win32
pywin32==306

# 🧰 Extras
Jinja2==3.1.4          # templating per Flask
itsdangerous==2.2.0    # richiesto da Flask per gestione sessioni
click==8.1.7           # CLI helper (dipendenza Flask)
